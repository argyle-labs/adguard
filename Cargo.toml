[package]
name = "adguard"
version = "0.1.0"
edition = "2021"
description = "AdGuard Home DNS sinkhole — orca service-backend"

[package.metadata.orca]
category = "networking"

[lib]
name = "adguard"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

# adguard is a BACKEND-ONLY orca plugin: zero #[orca_tool]s. It registers a
# ServiceBackend across the cdylib FFI seam and reaches the whole orca system
# through plugin-toolkit alone.
[dependencies]
plugin-toolkit = { git = "https://github.com/argyle-labs/orca", branch = "main", default-features = false, features = ["tools", "db"] }
# The ONE non-orca dep: #[export_root_module] expands to bare ::abi_stable paths.
abi_stable = "0.11"

[dev-dependencies]
plugin-toolkit = { git = "https://github.com/argyle-labs/orca", branch = "main", default-features = false, features = ["tools", "db"] }
tokio    = { version = "1", features = ["macros", "rt-multi-thread"] }
tempfile = "3"

[lints.clippy]
let_underscore_must_use = "deny"
